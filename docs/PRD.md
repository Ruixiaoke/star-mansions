# PRD — 二十八星宿测算 Web App（本命宿 · 《宿曜经》农历生日法）

> 状态：DRAFT 0.4 · 2026-07-19（**§7 核心测算法更正：值日法 → 《宿曜经》本命宿**，见 §7/§13/§15）
> 定位：这份 PRD 是本项目「产品的唯一真相源（Source of Truth）」—— 人和 AI 动手前都回这份读、这份改。
> 平台：响应式 Web App（React + Vite 前端 + 同一 Vercel 项目的 `api/` Serverless Functions 当后端），手机浏览器可用。
> 一句话：**输入公历或农历生日（年月日时）→ 算出你的本命星宿 → 看它的形象、星图与多维度解读。**

---

## 1. 目标（这个产品要解决什么）

让普通用户不用懂任何历法，只输入自己的生日，就能：

1. 知道自己的**本命星宿**（二十八宿之一）；
2. 看到这个星宿的**形象与星图**（视觉）；
3. 读到这个星宿的**多维度解读**：意义、本命、一生命运、性格、事业、财运、爱情。

并且登录后能**保存/回看**自己（及家人朋友）的测算结果。

> 心态定位：文化 + 娱乐向的自我认知工具，不是命理预测服务。红线见 §12。

---

## 2. 目标用户

- 对传统文化 / 星座命理 / 自我探索感兴趣的大众用户（年轻人为主）。
- 不懂农历换算、不懂二十八宿，只想「输入生日，看结果」。
- 会分享结果到社交媒体（截图 / 分享卡片是重要传播点）。

---

## 3. MVP 边界（先做什么，先不做什么）

### 核心动作（MVP 只保证这一个 Flow 顺畅）
> **输入生日 → 得到本命宿 → 看解读**。其余都围绕它。

### MVP 做
- 生日输入（公历/农历切换，年月日 + 时辰）。
- 本命宿测算（值日法，见 §7）。
- 结果页：星宿形象 + 星图 + 七个解读板块 + 时辰辅助板块（选了时辰才出）。
- 邮箱直登（登录 = 注册合一，见 §8）+ 保存我的测算记录、回看历史。
- 一份可分享的结果卡片（截图即可，不做复杂分享 SDK）。

### MVP 不做（后续）
- 付费 / 会员 / 解锁高级报告。
- 合盘（两人星宿配对）、流年运势、每日运势。
- AI 对话式解读、语音。
- 多语言（先中文）。
- 复杂社交（关注、社区、评论）。

---

## 4. 核心 Flow（主流程）

```
落地页
  → [公历 / 农历] 切换
  → 选 年 / 月 / 日 / 时辰（时辰可选“不确定”）
  → 点「测算」
  → 载入动画（排盘感）
  → 结果页：
       顶部：本命宿名（如「房日兔」）+ 所属四象（东方青龙）+ 星图/形象
       中部：七个解读板块（意义 / 本命 / 一生命运 / 性格 / 事业 / 财运 / 爱情）
       条件：时辰辅助板块（仅当填了时辰时显示）
       底部：保存（需登录）/ 生成分享卡片 / 再测一个
```

- 未登录也能测算和看结果；**保存/历史**才要求登录（降低首次体验门槛）。
- 时辰选「不确定」：本命宿照常算（值日宿按“日”定），时辰辅助板块隐藏。

---

## 5. 页面 Pages

| 页面 | 说明 |
|------|------|
| `Landing` 落地页 | 价值主张 + 生日输入入口（核心 CTA）|
| `Result` 结果页 | 本命宿 + 星图/形象 + 七大解读板块 + 时辰辅助板块 + 保存/分享 |
| `Login` 登录 | 填邮箱即登录（**登录 = 注册合一，无独立注册页**，见 §8）|
| `History` 我的测算 | 登录后：保存过的测算记录列表，可回看 |
| `XiuDetail`（可选）| 单个星宿的完整词条页（可与 Result 复用）|
| `About` 关于/免责 | 说明测算依据、免责声明、隐私说明（红线落地）|

> MVP 页面数尽量少：`Landing` + `Result` + 登录 + `History` + `About` 即可。

---

## 6. 组件 Components（可复用）

- `BirthdayInput` — 公历/农历切换 + 年月日时选择器（含「时辰不确定」）。
- `XiuBadge` — 星宿名 + 四象色 + 小图标。
- `StarMap` — 星图/星官图展示（图片或 SVG）。
- `XiuHero` — 结果页顶部形象区。
- `AnalysisSection` — 一个解读板块（标题 + 正文）；七大板块 + 时辰辅助板块都复用它。
- `ResultCard` — 可截图分享的结果卡片。
- `AuthForm` — 邮箱直登表单（单个邮箱输入框，提交即登录/建号；无注册切换，见 §8）。
- `HistoryList` / `HistoryItem`。

---

## 7. 测算模块（🔑 核心引擎：《宿曜经》本命宿 · 按农历生日）

> ⚠️ **重要更正（v0.4，2026-07-19）**：早期 PRD（§7/§13/§15 决策 1）把「本命宿」等同于老黄历的
> **「值日宿」**（每日轮值、逐年变，由 `lunar-javascript.getXiu()` 给出）——**这是概念性错误**。
> 老黄历「值日星宿」与《宿曜经》按农历生日算的「本命宿」**不是同一个概念**，很多站点混用导致结果打架。
> 经用户样本（农历四月初十在 1990/2005/2018 三年均为「轸水蚓」，逐年不变）+ 老黄历/查询站独立比对，
> 确认本项目应算的是**《宿曜经》本命宿**。已用 **12 个样本对权威源交叉校验全部命中**（见 §13）。

### 方法
**本命宿 = 依《宿曜经》按出生的「农历月 + 农历日」查定的命宿。** 与出生**年份、时辰无关**（公历输入先换算成农历）。

- 二十八宿（四象 × 七宿）：
  - 东方青龙：角 亢 氐 房 心 尾 箕
  - 北方玄武：斗 牛 女 虚 危 室 壁
  - 西方白虎：奎 娄 胃 昴 毕 觜 参
  - 南方朱雀：井 鬼 柳 星 张 翼 轸
- 《宿曜经》为 **二十七宿体系**（去「牛」宿 —— 印度尊牛，牛宿不值日）。因此本命宿取值**恒不含「牛」**；
  释义库「牛金牛」词条保留，但不会被本命宿命中。
- 每宿的传统属性（七政/七曜 + 动物，如「房日兔」「轸水蚓」）由固定「禽星表」给出（见下）。

### 输入 → 计算（《宿曜经》原文算法）
《宿曜经》原文：**「先下生日数，又虚加十三讫，即从彼生月望宿，用上位数顺除，数尽则止，即得彼人所属命宿。」**

1. 公历/农历输入 → `lunar-javascript` 统一换算出**农历月、农历日**。
2. 取该农历月的**望宿**（每月十五之宿）：
   正月翼 · 二月角 · 三月氐 · 四月心 · 五月箕 · 六月女 · 七月室 · 八月娄 · 九月昴 · 十月觜 · 冬月(十一)鬼 · 腊月(十二)星。
3. 从「望宿」起（望宿计为第 1 位），按 **27 宿序（娄首、去牛：
   娄胃昴毕觜参井鬼柳星张翼轸角亢氐房心尾箕斗女虚危室壁奎）** 顺数「**农历日 + 13**」位，落点即**本命宿**。
   - 自洽校验：农历十五（望日）恰好落回该月望宿。
4. 本命宿的**七政 + 动物**由固定「禽星表」给出（如 轸→水+蚓=轸水蚓）；四象/方位由定表给出。
5. **时辰 → 辅助解读（决策 3）**：用出生时辰（`lunar.getTimeZhi()` 取地支）驱动「时辰辅助」板块，
   内容取自释义库；选「不确定」则隐藏该板块，本命宿照常算（时辰不改变本命宿）。

### 实现方式
- `lunar-javascript`（6tail，MIT）仍负责**公历↔农历换算**与时辰地支 —— 成熟库，不手搓历法（不写死农历换算表）。
- 本命宿算法（望宿表 + 27 宿序 + 顺数 +13）**依《宿曜经》原文与其官方例子实现**（不臆造、非现编），
  封装成**可替换模块** `computeBenmingXiu(input) → { xiu, zheng, animal, siXiang, direction, timeZhi? }`。
- **闰月处理（待确认，§15-d）**：暂按同名本月处理（闰四月 = 四月望宿）。
- **交叉校验（已做，PRD §13）**：12 个样本对权威源（老黄历/查询站「本命星宿」字段 + 《宿曜经》官方例子）全部命中。

---

## 8. 登录 & 后端

- **目的**：邮箱登录 + 保存/回看测算记录（生日 + 结果），后续做个性化。
- **登录（已定：邮箱免验证直登 — 决策 2）**：只填邮箱即视为登录，**不发验证码 / magic link，也不自建密码体系**。
  - ⚠️ 邮箱在此**仅作软标识**（未验证所有权，任何人可填任意邮箱）——这是 MVP 降低体验门槛的**刻意取舍**；因此生日等敏感数据的保护以「未登录不强制留存 + 用户可删除」为准（见 §12），不把「已登录」当成身份可信。
- **注册 = 与登录合一（无独立注册步骤）**：免验证直登下**不区分「注册」和「登录」**——首次填入某邮箱即**自动建号**，之后再填同一邮箱即**登录同一账号**。前端只有 `authAdapter.login(email)` 一个动作兼顾建号 / 登录（后端对该邮箱做 upsert）；不存在单独的注册页 / 注册表单。
- **后端 = 同一个 Vercel 项目里的 `api/` Serverless Functions**（跟前端同仓、同一次部署、同一个域名，不另起服务）：
  - Functions 提供 API 端点：保存 / 读取 `Reading`、按邮箱维持会话等。
  - **会话 & 存储**用成熟方案对接（Supabase 或 Vercel Postgres/KV，见 §15）；免验证直登**不需要邮件通道（OTP / magic link）**，Functions 只负责登记邮箱、维持会话、读写记录 —— 仍是 API 层，**不从零手搓鉴权**。
  - **数据存储**：Vercel Postgres / KV，或 Supabase Postgres（二选一，见 §15）。
- **前端只依赖两个接口**：`authAdapter`（login / logout / currentUser）+ `readingStore`（save / list）—— 后端实现（Functions + auth + 存储）可整体替换，不影响页面。
- **配合 L4**：scaffold 阶段这两个接口先做**占位**（本地存储 / mock）；真后端（Vercel Functions + auth + DB）放到 Vercel 上，作为**下一步动态能力**接入 —— 正好是 L4 收尾说的「动态能力后续在 Vercel 扩展」。
- **账号数据最小化**：只存必要标识（邮箱）+ 用户主动保存的记录；生日属敏感信息（见 §12 隐私红线）。

---

## 9. 数据模型 Data（先 mock / fixture，后接真实）

```
User        { id, email, createdAt }
XiuEntry    { id(宿名), 名称, 四象, 七政, 动物,
              formImage, starMap,
              意义, 本命, 一生命运, 性格, 事业, 财运, 爱情,
              时辰辅助?: { [地支时]: 文案 } }              // = 释义库一条
Reading     { id, userId?, 输入{历法, 年,月,日,时辰}, 公历date, benmingXiuId, createdAt }
```

- `XiuEntry` × 28 条 = **释义内容库**（§10），静态数据，前端可直接读 JSON。
- 七政 / 动物既可由库实时算，也可冗余进 `XiuEntry` 便于展示；以库为准。
- `Reading` = 一次测算结果；登录后与 `userId` 绑定进入 `History`。
- MVP 阶段：`XiuEntry` 用真实整理好的内容 JSON；`User`/`Reading` 走 `authAdapter` / `readingStore` 接口（真实现 = 同仓 Vercel Functions + 存储；登录前可临时存本地）。

---

## 10. 内容资产（释义库 + 图）

- **28 宿 × 九类内容**（形象、星图、意义、本命、一生命运、性格、事业、财运、爱情）+ 时辰辅助文案 = 结构化 JSON（每宿一条 `XiuEntry`）。
- **MVP 上全真内容（决策 4）**：28 宿全部按真实传统资料整理齐，**不留占位**。这是 MVP 的一项硬内容工作量，需排期。
- **🚨 内容纪律**：解读文案**基于可查的传统资料整理、来源可追溯，不由 AI 现编冒充权威**。可用 AI 辅助起草，但必须核对传统出处、标注来源、人工过一遍定稿。
- **图片：AI 模型生成（决策 5）**：形象图 / 星图由生图模型产出，统一风格（东方 + 星空），28 宿一致。
  - 版权：生成图作为原创素材使用；选用**允许商用**的模型 / 授权，记录所用模型与生成参数（可追溯）。
  - ⚠️ **两类图要分清**：①**星宿形象图**（艺术形象）——AI 生成没问题、正是它擅长的；②**星图**——若要表达**真实星官位置**（天文准确），生图模型会画错、不可信；本项目定位是文化娱乐，**星图按「装饰性 / 示意」处理，不宣称天文精确**（若日后要精确星官，另基于真实星表坐标绘制）。

---

## 11. 设计 / 视觉 SoT（tokens）

- 复用「视觉真相源」`tokens.css`：颜色 / 字体 / 间距 / 圆角 / 阴影统一变量化。
- 风格方向：东方 + 星空感（四象四色可作分类色：青龙-青、朱雀-红、白虎-白/金、玄武-黑/蓝）。**具体色板在 tokens 里定，页面只引变量。**
- AI 生成的形象图 / 星图与 tokens 色系要协调（生成时把四象色带进 prompt，保持整套视觉统一）。
- 结果卡片要考虑「适合截图分享」的构图。

---

## 12. 红线（绝不违反）

1. **文化娱乐定位**：结果页与 About 页必须有**显著免责声明** —— 内容为传统文化 / 娱乐参考，不构成人生、医疗、投资、婚姻等任何决策建议。
2. **不臆造命理内容**：解读文案基于可查资料整理、标来源，不 AI 现编冒充权威（§10）；测算用成熟历法库并交叉校验，不写死可能错误的表（§7）。
3. **隐私**：生日 + 邮箱是敏感个人信息 —— 明确告知用途、未登录不强制留存、登录用户可删除自己的记录与账号；不向第三方售卖。
4. **不制造焦虑 / 不诱导付费迷信**：不用「命中注定破财 / 必须化解」这类恐吓式文案（MVP 也不含付费）。
5. **合规**：AI 生成图选允许商用的模型 / 授权并记录来源，不盗用他人素材。

---

## 13. 验收标准（人工可判断，做完对照这个）

- [ ] 输入公历生日能算出本命宿；切换农历、同一个人（对应同一天）算出**同一个宿**。
- [x] 用 **≥3 个已知本命宿的样本**核对本命宿算法（《宿曜经》），结果正确 —— **已做，12/12 命中**：
  - 农历四月初十 → **轸水蚓**（1990-05-04 / 2005-05-17 / 2018-05-24 三年同值，证年份无关）
  - 农历四月十四 → **房日兔**；农历七月十五 → **室火猪**；农历十月初六 → **虚日鼠**；农历正月初六 → **昴日鸡**
  - 《宿曜经》官方例：九月初十→室、七月十一→斗、五月初五→星；万年历正月序：初一室·初二壁·初三奎
  - 权威源：老黄历/查询站「本命星宿」字段（如 bmcx / nuoshai）+《宿曜经》原文例子。
- [ ] 时辰选「不确定」也能出本命宿、不报错；**填了时辰会多出一个时辰辅助解读板块**。
- [ ] **全 28 宿**七大板块内容齐全（全真、无占位）；星图/形象图（AI 生成）都正常显示、风格统一。
- [ ] 未登录可测算看结果；点「保存」引导邮箱登录；登录后能在 History 回看。
- [ ] 页面全程使用 `tokens.css` 变量，风格统一。
- [ ] 免责声明在结果页/About 页可见；隐私说明存在；账号/记录可删除。
- [ ] 手机浏览器打开体验正常（响应式）。

---

## 14. MVP 之外（后续路线，先记着不做）

- 付费高级报告 / 会员。
- 合盘配对、流年 / 每日运势、时辰精细化解读。
- AI 对话式解读、分享 SDK、社区。
- 多语言。
- 天文精确星图（基于真实星表坐标）。

---

## 15. 决策记录 & 剩余待确认

**已定（本轮 Rick 拍板）**
1. 测算法 = **《宿曜经》本命宿（按农历生日）**。`lunar-javascript`（6tail）负责公历↔农历换算与时辰地支；
   本命宿按《宿曜经》原文算法（望宿表 + 27 宿序 + 顺数 +13）实现并 12 样本交叉校验。
   ~~（原 0.3：本命宿 = lunar-javascript 值日宿 —— 已于 v0.4 更正，值日宿是另一个概念，见 §7 更正框 / 决策 7）~~
2. 登录 = **邮箱免验证直登**（只填邮箱即登录，不发验证码 / magic link，不自建密码；邮箱仅作软标识，MVP 降门槛取舍，见 §8）。
3. 时辰 = **真接入一个「时辰辅助解读」板块**（不是纯预留）。
4. 释义库 = MVP **上全 28 宿真实内容**（不留占位）。
5. 星图 / 形象图 = **AI 模型生成**（星图按装饰/示意处理，不宣称天文精确）。

**已定（补充）**
6. 后端 = **同仓 Vercel Functions（`api/`）** 当 API 层 + 前端两个接口 `authAdapter` / `readingStore` 兜底；真后端作为下一步在 Vercel 接入。
7. **（2026-07-19 更正）核心测算法：值日法 → 《宿曜经》本命宿。** 起因：线上结果与老黄历/查询站「本命星宿」
   不符（用户样本 1990-05-04 应为轸水蚓，旧算法给娄金狗——娄是「值日宿」）。确认「值日宿 ≠ 本命宿」，
   改用《宿曜经》按农历生日的本命宿算法，12 样本交叉校验通过（§13）。**连带需 Rick 明确确认的铁律改动**：
   CLAUDE.md §0-2「测算不手写值日表、一律走 lunar-javascript」的措辞需更新为「走可查权威方法（宿曜经算法 /
   lunar-javascript 换算）、不臆造、≥3 样本交叉校验」——本 PR 已改，待你 review 时确认（改铁律须你明确点头）。

**还需你定（小项，不阻塞开工）**
- a. Functions 背后的 **存储** 选型：**Supabase Postgres** 还是 **Vercel Postgres / KV**？（免验证直登后不再需要托管认证的邮件通道，选型重点从「auth」回到「存储 + 会话」；建议 Supabase：一站省事，Functions 里调它的 SDK）
- b. 时辰辅助板块解读**哪个维度**：时辰对性格的微调？还是时辰宜忌？（**现按「性格微调」实现**，见 `readings.ts` TIME_HELP，待最终确认）
- c. 生图用**哪个模型**、什么统一画风（写死一句 style prompt 好让 28 张一致）？允许商用的授权确认。
- d. **闰月本命宿处理**：闰 X 月按「X 月」的望宿算（现行）还是另设规则？（现暂按同名本月，待确认）
