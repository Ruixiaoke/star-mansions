# CORS 允许的前端来源
# 本地：Vite dev server；生产：GitHub Pages 域名（如 https://<user>.github.io）
ALLOWED_ORIGIN=http://localhost:5173

# 本地 Express dev server 端口
PORT=3000

# ── 存储（占位，scaffold 阶段不需要真值；PRD §15-a 选型后再启用）──
# 铁律 §0-3：真实密钥写进 backend/.env（已被 .gitignore 忽略），绝不提交
# SUPABASE_URL=
# SUPABASE_ANON_KEY=
